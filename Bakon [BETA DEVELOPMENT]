local Rayfield = loadstring(game:HttpGet('https://sirius.menu/rayfield'))()
local window = Rayfield:CreateWindow({
    Name = "Bakon",
    LoadingTitle = "Loading Assets..",
    LoadingSubtitle = "Created By Derpixh",
    ConfigurationSaving = {
        Enabled = false,
        FileName = "Sirius"
    },
    KeySystem = true,
    KeySettings = {
        Title = "Derpixh's Key System",
        Subtitle = "Enter Your Key",
        Note = "Rate The Features",
        FileName = "Sirius",
        SaveKey = false,
        Key = "2025"
    }
})

local chapter1Tab = window:CreateTab("Chapter 1", 4483362458)

local booksSection = chapter1Tab:CreateSection("Books")

chapter1Tab:CreateButton({
    Name = "Grab Green Book",
    Callback = function()
        local CurrentMap = game.Workspace:FindFirstChild("CurrentMap")
        if CurrentMap then
            local Chapter1 = CurrentMap:FindFirstChild("Chapter1")
            if Chapter1 then
                local Utilities = Chapter1:FindFirstChild("Utilities")
                if Utilities then
                    local GreenBook = Utilities:FindFirstChild("Green Book")
                    if GreenBook then
                        local Bounds = GreenBook:FindFirstChild("Bounds")
                        if Bounds then
                            local ClickDetector = Bounds:FindFirstChild("PickUp")
                            if ClickDetector and ClickDetector:IsA("ClickDetector") then
                                fireclickdetector(ClickDetector)
                            end
                        end
                    end
                end
            end
        end
    end,
    SectionParent = booksSection
})

chapter1Tab:CreateButton({
    Name = "Grab Blue Book",
    Callback = function()
        local CurrentMap = game.Workspace:FindFirstChild("CurrentMap")
        if CurrentMap then
            local Chapter1 = CurrentMap:FindFirstChild("Chapter1")
            if Chapter1 then
                local Utilities = Chapter1:FindFirstChild("Utilities")
                if Utilities then
                    local BlueBook = Utilities:FindFirstChild("Blue Book")
                    if BlueBook then
                        local Bounds = BlueBook:FindFirstChild("Bounds")
                        if Bounds then
                            local ClickDetector = Bounds:FindFirstChild("PickUp")
                            if ClickDetector and ClickDetector:IsA("ClickDetector") then
                                fireclickdetector(ClickDetector)
                            end
                        end
                    end
                end
            end
        end
    end,
    SectionParent = booksSection
})

chapter1Tab:CreateButton({
    Name = "Grab Red Book",
    Callback = function()
        local CurrentMap = game.Workspace:FindFirstChild("CurrentMap")
        if CurrentMap then
            local Chapter1 = CurrentMap:FindFirstChild("Chapter1")
            if Chapter1 then
                local Utilities = Chapter1:FindFirstChild("Utilities")
                if Utilities then
                    local RedBook = Utilities:FindFirstChild("Red Book")
                    if RedBook then
                        local Bounds = RedBook:FindFirstChild("Bounds")
                        if Bounds then
                            local ClickDetector = Bounds:FindFirstChild("PickUp")
                            if ClickDetector and ClickDetector:IsA("ClickDetector") then
                                fireclickdetector(ClickDetector)
                            end
                        end
                    end
                end
            end
        end
    end,
    SectionParent = booksSection
})

local wrenchSection = chapter1Tab:CreateSection("Wrench")
chapter1Tab:CreateButton({
    Name = "Grab Wrench",
    Callback = function()
        local CurrentMap = game.Workspace:FindFirstChild("CurrentMap")
        if CurrentMap then
            local Chapter1 = CurrentMap:FindFirstChild("Chapter1")
            if Chapter1 then
                local Utilities = Chapter1:FindFirstChild("Utilities")
                if Utilities then
                    local Wrench = Utilities:FindFirstChild("Wrench")
                    if Wrench then
                        local Bounds = Wrench:FindFirstChild("Bounds")
                        if Bounds then
                            local ClickDetector = Bounds:FindFirstChild("PickUp")
                            if ClickDetector and ClickDetector:IsA("ClickDetector") then
                                fireclickdetector(ClickDetector)
                            end
                        end
                    end
                end
            end
        end
    end,
    SectionParent = wrenchSection
})

local crowbarSection = chapter1Tab:CreateSection("Crowbar")

chapter1Tab:CreateButton({
    Name = "Grab Crowbar",
    Callback = function()
        local CurrentMap = game.Workspace:FindFirstChild("CurrentMap")
        if CurrentMap then
            local Chapter1 = CurrentMap:FindFirstChild("Chapter1")
            if Chapter1 then
                local Utilities = Chapter1:FindFirstChild("Utilities")
                if Utilities then
                    local Crowbar = Utilities:FindFirstChild("Crowbar")
                    if Crowbar then
                        local Bounds = Crowbar:FindFirstChild("Bounds")
                        if Bounds then
                            local ClickDetector = Bounds:FindFirstChild("PickUp")
                            if ClickDetector and ClickDetector:IsA("ClickDetector") then
                                fireclickdetector(ClickDetector)
                            end
                        end
                    end
                end
            end
        end
    end,
    SectionParent = crowbarSection
})

local trussSection = chapter1Tab:CreateSection("Truss")

chapter1Tab:CreateButton({
    Name = "Grab Truss",
    Callback = function()
        local CurrentMap = game.Workspace:FindFirstChild("CurrentMap")
        if CurrentMap then
            local Chapter1 = CurrentMap:FindFirstChild("Chapter1")
            if Chapter1 then
                local Utilities = Chapter1:FindFirstChild("Utilities")
                if Utilities then
                    local Truss = Utilities:FindFirstChild("Truss")
                    if Truss then
                        local Bounds = Truss:FindFirstChild("Bounds")
                        if Bounds then
                            local ClickDetector = Bounds:FindFirstChild("PickUp")
                            if ClickDetector and ClickDetector:IsA("ClickDetector") then
                                fireclickdetector(ClickDetector)
                            end
                        end
                    end
                end
            end
        end
    end,
    SectionParent = trussSection
})

local usbSection = chapter1Tab:CreateSection("USB")

chapter1Tab:CreateButton({
    Name = "Grab USB",
    Callback = function()
        local CurrentMap = game.Workspace:FindFirstChild("CurrentMap")
        if CurrentMap then
            local Chapter1 = CurrentMap:FindFirstChild("Chapter1")
            if Chapter1 then
                local Utilities = Chapter1:FindFirstChild("Utilities")
                if Utilities then
                    local USB = Utilities:FindFirstChild("USB")
                    if USB then
                        local Bounds = USB:FindFirstChild("Bounds")
                        if Bounds then
                            local ClickDetector = Bounds:FindFirstChild("PickUp")
                            if ClickDetector and ClickDetector:IsA("ClickDetector") then
                                fireclickdetector(ClickDetector)
                            end
                        end
                    end
                end
            end
        end
    end,
    SectionParent = usbSection
})

local whiteKeySection = chapter1Tab:CreateSection("White Key")

chapter1Tab:CreateButton({
    Name = "Grab White Key",
    Callback = function()
        local CurrentMap = game.Workspace:FindFirstChild("CurrentMap")
        if CurrentMap then
            local Chapter1 = CurrentMap:FindFirstChild("Chapter1")
            if Chapter1 then
                local Utilities = Chapter1:FindFirstChild("Utilities")
                if Utilities then
                    local WhiteKey = Utilities:FindFirstChild("White Key")
                    if WhiteKey then
                        local Bounds = WhiteKey:FindFirstChild("Bounds")
                        if Bounds then
                            local ClickDetector = Bounds:FindFirstChild("PickUp")
                            if ClickDetector and ClickDetector:IsA("ClickDetector") then
                                fireclickdetector(ClickDetector)
                            end
                        end
                    end
                end
            end
        end
    end,
    SectionParent = whiteKeySection
})

local taserSection = chapter1Tab:CreateSection("Taser")

chapter1Tab:CreateButton({
    Name = "Grab Taser",
    Callback = function()
        local CurrentMap = game.Workspace:FindFirstChild("CurrentMap")
        if CurrentMap then
            local Chapter1 = CurrentMap:FindFirstChild("Chapter1")
            if Chapter1 then
                local Utilities = Chapter1:FindFirstChild("Utilities")
                if Utilities then
                    local Taser = Utilities:FindFirstChild("Taser")
                    if Taser then
                        local Bounds = Taser:FindFirstChild("Bounds")
                        if Bounds then
                            local ClickDetector = Bounds:FindFirstChild("PickUp")
                            if ClickDetector and ClickDetector:IsA("ClickDetector") then
                                fireclickdetector(ClickDetector)
                            end
                        end
                    end
                end
            end
        end
    end,
    SectionParent = taserSection
})

local chapter2Tab = window:CreateTab("Chapter 2", 4483362458)

local wrenchSection = chapter2Tab:CreateSection("Wrench")

chapter2Tab:CreateButton({
    Name = "Grab Wrench",
    Callback = function()
        local CurrentMap = game.Workspace:FindFirstChild("CurrentMap")
        if CurrentMap then
            local Chapter2 = CurrentMap:FindFirstChild("Chapter2")
            if Chapter2 then
                local Utilities = Chapter2:FindFirstChild("Utilities")
                if Utilities then
                    local Wrench = Utilities:FindFirstChild("Wrench")
                    if Wrench then
                        local Bounds = Wrench:FindFirstChild("Bounds")
                        if Bounds then
                            local ClickDetector = Bounds:FindFirstChild("PickUp")
                            if ClickDetector and ClickDetector:IsA("ClickDetector") then
                                fireclickdetector(ClickDetector)
                            end
                        end
                    end
                end
            end
        end
    end,
    SectionParent = wrenchSection
})

local redKeySection = chapter2Tab:CreateSection("Red Key")

chapter2Tab:CreateButton({
    Name = "Grab Red Key",
    Callback = function()
        local CurrentMap = game.Workspace:FindFirstChild("CurrentMap")
        if CurrentMap then
            local Chapter2 = CurrentMap:FindFirstChild("Chapter2")
            if Chapter2 then
                local Utilities = Chapter2:FindFirstChild("Utilities")
                if Utilities then
                    local RedKey = Utilities:FindFirstChild("Red Key")
                    if RedKey then
                        local Bounds = RedKey:FindFirstChild("Bounds")
                        if Bounds then
                            local ClickDetector = Bounds:FindFirstChild("PickUp")
                            if ClickDetector and ClickDetector:IsA("ClickDetector") then
                                fireclickdetector(ClickDetector)
                            end
                        end
                    end
                end
            end
        end
    end,
    SectionParent = redKeySection
})

local crowbarSection = chapter2Tab:CreateSection("Crowbar")

chapter2Tab:CreateButton({
    Name = "Grab Crowbar",
    Callback = function()
        local CurrentMap = game.Workspace:FindFirstChild("CurrentMap")
        if CurrentMap then
            local Chapter2 = CurrentMap:FindFirstChild("Chapter2")
            if Chapter2 then
                local Utilities = Chapter2:FindFirstChild("Utilities")
                if Utilities then
                    local Crowbar = Utilities:FindFirstChild("Crowbar")
                    if Crowbar then
                        local Bounds = Crowbar:FindFirstChild("Bounds")
                        if Bounds then
                            local ClickDetector = Bounds:FindFirstChild("PickUp")
                            if ClickDetector and ClickDetector:IsA("ClickDetector") then
                                fireclickdetector(ClickDetector)
                            end
                        end
                    end
                end
            end
        end
    end,
    SectionParent = crowbarSection
})

local wiresSection = chapter2Tab:CreateSection("Wires")

chapter2Tab:CreateButton({
    Name = "Grab Red Wire",
    Callback = function()
        local CurrentMap = game.Workspace:FindFirstChild("CurrentMap")
        if CurrentMap then
            local Chapter2 = CurrentMap:FindFirstChild("Chapter2")
            if Chapter2 then
                local Utilities = Chapter2:FindFirstChild("Utilities")
                if Utilities then
                    local RedWire = Utilities:FindFirstChild("Red Wire")
                    if RedWire then
                        local Bounds = RedWire:FindFirstChild("Bounds")
                        if Bounds then
                            local ClickDetector = Bounds:FindFirstChild("PickUp")
                            if ClickDetector and ClickDetector:IsA("ClickDetector") then
                                fireclickdetector(ClickDetector)
                            end
                        end
                    end
                end
            end
        end
    end,
    SectionParent = wiresSection
})

chapter2Tab:CreateButton({
    Name = "Grab Green Wire",
    Callback = function()
        local CurrentMap = game.Workspace:FindFirstChild("CurrentMap")
        if CurrentMap then
            local Chapter2 = CurrentMap:FindFirstChild("Chapter2")
            if Chapter2 then
                local Utilities = Chapter2:FindFirstChild("Utilities")
                if Utilities then
                    local GreenWire = Utilities:FindFirstChild("Green Wire")
                    if GreenWire then
                        local Bounds = GreenWire:FindFirstChild("Bounds")
                        if Bounds then
                            local ClickDetector = Bounds:FindFirstChild("PickUp")
                            if ClickDetector and ClickDetector:IsA("ClickDetector") then
                                fireclickdetector(ClickDetector)
                            end
                        end
                    end
                end
            end
        end
    end,
    SectionParent = wiresSection
})

local goldenGearSection = chapter2Tab:CreateSection("Golden Gear")

chapter2Tab:CreateButton({
    Name = "Grab Golden Gear",
    Callback = function()
        local CurrentMap = game.Workspace:FindFirstChild("CurrentMap")
        if CurrentMap then
            local Chapter2 = CurrentMap:FindFirstChild("Chapter2")
            if Chapter2 then
                local Utilities = Chapter2:FindFirstChild("Utilities")
                if Utilities then
                    local GoldenGear = Utilities:FindFirstChild("Golden Gear")
                    if GoldenGear then
                        local Bounds = GoldenGear:FindFirstChild("Bounds")
                        if Bounds then
                            local ClickDetector = Bounds:FindFirstChild("PickUp")
                            if ClickDetector and ClickDetector:IsA("ClickDetector") then
                                fireclickdetector(ClickDetector)
                            end
                        end
                    end
                end
            end
        end
    end,
    SectionParent = goldenGearSection
})

local whiteKeySection = chapter2Tab:CreateSection("White Key")

chapter2Tab:CreateButton({
    Name = "Grab White Key",
    Callback = function()
        local CurrentMap = game.Workspace:FindFirstChild("CurrentMap")
        if CurrentMap then
            local Chapter2 = CurrentMap:FindFirstChild("Chapter2")
            if Chapter2 then
                local Utilities = Chapter2:FindFirstChild("Utilities")
                if Utilities then
                    local WhiteKey = Utilities:FindFirstChild("White Key")
                    if WhiteKey then
                        local Bounds = WhiteKey:FindFirstChild("Bounds")
                        if Bounds then
                            local ClickDetector = Bounds:FindFirstChild("PickUp")
                            if ClickDetector and ClickDetector:IsA("ClickDetector") then
                                fireclickdetector(ClickDetector)
                            end
                        end
                    end
                end
            end
        end
    end,
    SectionParent = whiteKeySection
})

local taserSection = chapter2Tab:CreateSection("Taser")

chapter2Tab:CreateButton({
    Name = "Grab Taser",
    Callback = function()
        local CurrentMap = game.Workspace:FindFirstChild("CurrentMap")
        if CurrentMap then
            local Chapter2 = CurrentMap:FindFirstChild("Chapter2")
            if Chapter2 then
                local Utilities = Chapter2:FindFirstChild("Utilities")
                if Utilities then
                    local Taser = Utilities:FindFirstChild("Taser")
                    if Taser then
                        local Bounds = Taser:FindFirstChild("Bounds")
                        if Bounds then
                            local ClickDetector = Bounds:FindFirstChild("PickUp")
                            if ClickDetector and ClickDetector:IsA("ClickDetector") then
                                fireclickdetector(ClickDetector)
                            end
                        end
                    end
                end
            end
        end
    end,
    SectionParent = taserSection
})

local chapter3Tab = window:CreateTab("Chapter 3", 4483362458)

local leverSection = chapter3Tab:CreateSection("Lever")

chapter3Tab:CreateButton({
    Name = "Grab Lever",
    Callback = function()
        local CurrentMap = game.Workspace:FindFirstChild("CurrentMap")
        if CurrentMap then
            local Chapter3 = CurrentMap:FindFirstChild("Chapter3")
            if Chapter3 then
                local Utilities = Chapter3:FindFirstChild("Utilities")
                if Utilities then
                    local Lever = Utilities:FindFirstChild("Lever")
                    if Lever then
                        local Bounds = Lever:FindFirstChild("Bounds")
                        if Bounds then
                            local ClickDetector = Bounds:FindFirstChild("PickUp")
                            if ClickDetector and ClickDetector:IsA("ClickDetector") then
                                fireclickdetector(ClickDetector)
                            end
                        end
                    end
                end
            end
        end
    end,
    SectionParent = leverSection
})

local valvesSection = chapter3Tab:CreateSection("Valves")

chapter3Tab:CreateButton({
    Name = "Grab White Valve",
    Callback = function()
        local CurrentMap = game.Workspace:FindFirstChild("CurrentMap")
        if CurrentMap then
            local Chapter3 = CurrentMap:FindFirstChild("Chapter3")
            if Chapter3 then
                local Utilities = Chapter3:FindFirstChild("Utilities")
                if Utilities then
                    local WhiteValve = Utilities:FindFirstChild("White Valve")
                    if WhiteValve then
                        local Bounds = WhiteValve:FindFirstChild("Bounds")
                        if Bounds then
                            local ClickDetector = Bounds:FindFirstChild("PickUp")
                            if ClickDetector and ClickDetector:IsA("ClickDetector") then
                                fireclickdetector(ClickDetector)
                            end
                        end
                    end
                end
            end
        end
    end,
    SectionParent = valvesSection
})

chapter3Tab:CreateButton({
    Name = "Grab Yellow Valve",
    Callback = function()
        local CurrentMap = game.Workspace:FindFirstChild("CurrentMap")
        if CurrentMap then
            local Chapter3 = CurrentMap:FindFirstChild("Chapter3")
            if Chapter3 then
                local Utilities = Chapter3:FindFirstChild("Utilities")
                if Utilities then
                    local YellowValve = Utilities:FindFirstChild("Yellow Valve")
                    if YellowValve then
                        local Bounds = YellowValve:FindFirstChild("Bounds")
                        if Bounds then
                            local ClickDetector = Bounds:FindFirstChild("PickUp")
                            if ClickDetector and ClickDetector:IsA("ClickDetector") then
                                fireclickdetector(ClickDetector)
                            end
                        end
                    end
                end
            end
        end
    end,
    SectionParent = valvesSection
})

local plankSection = chapter3Tab:CreateSection("Plank")

chapter3Tab:CreateButton({
    Name = "Grab Plank",
    Callback = function()
        local CurrentMap = game.Workspace:FindFirstChild("CurrentMap")
        if CurrentMap then
            local Chapter3 = CurrentMap:FindFirstChild("Chapter3")
            if Chapter3 then
                local Utilities = Chapter3:FindFirstChild("Utilities")
                if Utilities then
                    local Plank = Utilities:FindFirstChild("Plank")
                    if Plank then
                        local Bounds = Plank:FindFirstChild("Bounds")
                        if Bounds then
                            local ClickDetector = Bounds:FindFirstChild("PickUp")
                            if ClickDetector and ClickDetector:IsA("ClickDetector") then
                                fireclickdetector(ClickDetector)
                            end
                        end
                    end
                end
            end
        end
    end,
    SectionParent = plankSection
})

local crowbarSection = chapter3Tab:CreateSection("Crowbar")

chapter3Tab:CreateButton({
    Name = "Grab Crowbar",
    Callback = function()
        local CurrentMap = game.Workspace:FindFirstChild("CurrentMap")
        if CurrentMap then
            local Chapter3 = CurrentMap:FindFirstChild("Chapter3")
            if Chapter3 then
                local Utilities = Chapter3:FindFirstChild("Utilities")
                if Utilities then
                    local Crowbar = Utilities:FindFirstChild("Crowbar")
                    if Crowbar then
                        local Bounds = Crowbar:FindFirstChild("Bounds")
                        if Bounds then
                            local ClickDetector = Bounds:FindFirstChild("PickUp")
                            if ClickDetector and ClickDetector:IsA("ClickDetector") then
                                fireclickdetector(ClickDetector)
                            end
                        end
                    end
                end
            end
        end
    end,
    SectionParent = crowbarSection
})

local thermiteSection = chapter3Tab:CreateSection("Thermite")

chapter3Tab:CreateButton({
    Name = "Grab Thermite",
    Callback = function()
        local CurrentMap = game.Workspace:FindFirstChild("CurrentMap")
        if CurrentMap then
            local Chapter3 = CurrentMap:FindFirstChild("Chapter3")
            if Chapter3 then
                local Utilities = Chapter3:FindFirstChild("Utilities")
                if Utilities then
                    local Thermite = Utilities:FindFirstChild("Thermite")
                    if Thermite then
                        local Bounds = Thermite:FindFirstChild("Bounds")
                        if Bounds then
                            local ClickDetector = Bounds:FindFirstChild("PickUp")
                            if ClickDetector and ClickDetector:IsA("ClickDetector") then
                                fireclickdetector(ClickDetector)
                            end
                        end
                    end
                end
            end
        end
    end,
    SectionParent = thermiteSection
})

local taserSection = chapter3Tab:CreateSection("Taser")

chapter3Tab:CreateButton({
    Name = "Grab Taser",
    Callback = function()
        local CurrentMap = game.Workspace:FindFirstChild("CurrentMap")
        if CurrentMap then
            local Chapter3 = CurrentMap:FindFirstChild("Chapter3")
            if Chapter3 then
                local Utilities = Chapter3:FindFirstChild("Utilities")
                if Utilities then
                    local Taser = Utilities:FindFirstChild("Taser")
                    if Taser then
                        local Bounds = Taser:FindFirstChild("Bounds")
                        if Bounds then
                            local ClickDetector = Bounds:FindFirstChild("PickUp")
                            if ClickDetector and ClickDetector:IsA("ClickDetector") then
                                fireclickdetector(ClickDetector)
                            end
                        end
                    end
                end
            end
        end
    end,
    SectionParent = taserSection
})

local chapter4Tab = window:CreateTab("Chapter 4", 4483362458)

local fireExtinguisherSection = chapter4Tab:CreateSection("Fire Extinguisher")

chapter4Tab:CreateButton({
    Name = "Grab Fire Extinguisher",
    Callback = function()
        local CurrentMap = game.Workspace:FindFirstChild("CurrentMap")
        if CurrentMap then
            local Chapter4 = CurrentMap:FindFirstChild("Chapter4")
            if Chapter4 then
                local Utilities = Chapter4:FindFirstChild("Utilities")
                if Utilities then
                    local FireExtinguisher = Utilities:FindFirstChild("FireExtinguisher")
                    if FireExtinguisher then
                        local Bounds = FireExtinguisher:FindFirstChild("Bounds")
                        if Bounds then
                            local ClickDetector = Bounds:FindFirstChild("PickUp")
                            if ClickDetector and ClickDetector:IsA("ClickDetector") then
                                fireclickdetector(ClickDetector)
                            end
                        end
                    end
                end
            end
        end
    end,
    SectionParent = fireExtinguisherSection
})

local wiresSection = chapter4Tab:CreateSection("Wires")

chapter4Tab:CreateButton({
    Name = "Grab Red Wire",
    Callback = function()
        local CurrentMap = game.Workspace:FindFirstChild("CurrentMap")
        if CurrentMap then
            local Chapter4 = CurrentMap:FindFirstChild("Chapter4")
            if Chapter4 then
                local Utilities = Chapter4:FindFirstChild("Utilities")
                if Utilities then
                    local RedWire = Utilities:FindFirstChild("Red Wire")
                    if RedWire then
                        local Bounds = RedWire:FindFirstChild("Bounds")
                        if Bounds then
                            local ClickDetector = Bounds:FindFirstChild("PickUp")
                            if ClickDetector and ClickDetector:IsA("ClickDetector") then
                                fireclickdetector(ClickDetector)
                            end
                        end
                    end
                end
            end
        end
    end,
    SectionParent = wiresSection
})

chapter4Tab:CreateButton({
    Name = "Grab Green Wire",
    Callback = function()
        local CurrentMap = game.Workspace:FindFirstChild("CurrentMap")
        if CurrentMap then
            local Chapter4 = CurrentMap:FindFirstChild("Chapter4")
            if Chapter4 then
                local Utilities = Chapter4:FindFirstChild("Utilities")
                if Utilities then
                    local GreenWire = Utilities:FindFirstChild("Green Wire")
                    if GreenWire then
                        local Bounds = GreenWire:FindFirstChild("Bounds")
                        if Bounds then
                            local ClickDetector = Bounds:FindFirstChild("PickUp")
                            if ClickDetector and ClickDetector:IsA("ClickDetector") then
                                fireclickdetector(ClickDetector)
                            end
                        end
                    end
                end
            end
        end
    end,
    SectionParent = wiresSection
})

local crowbarSection = chapter4Tab:CreateSection("Crowbar")

chapter4Tab:CreateButton({
    Name = "Grab Crowbar",
    Callback = function()
        local CurrentMap = game.Workspace:FindFirstChild("CurrentMap")
        if CurrentMap then
            local Chapter4 = CurrentMap:FindFirstChild("Chapter4")
            if Chapter4 then
                local Utilities = Chapter4:FindFirstChild("Utilities")
                if Utilities then
                    local Crowbar = Utilities:FindFirstChild("Crowbar")
                    if Crowbar then
                        local Bounds = Crowbar:FindFirstChild("Bounds")
                        if Bounds then
                            local ClickDetector = Bounds:FindFirstChild("PickUp")
                            if ClickDetector and ClickDetector:IsA("ClickDetector") then
                                fireclickdetector(ClickDetector)
                            end
                        end
                    end
                end
            end
        end
    end,
    SectionParent = crowbarSection
})

local pinkKeySection = chapter4Tab:CreateSection("Pink Key")

chapter4Tab:CreateButton({
    Name = "Grab Pink Key",
    Callback = function()
        local CurrentMap = game.Workspace:FindFirstChild("CurrentMap")
        if CurrentMap then
            local Chapter4 = CurrentMap:FindFirstChild("Chapter4")
            if Chapter4 then
                local Utilities = Chapter4:FindFirstChild("Utilities")
                if Utilities then
                    local PinkKey = Utilities:FindFirstChild("Pink Key")
                    if PinkKey then
                        local Bounds = PinkKey:FindFirstChild("Bounds")
                        if Bounds then
                            local ClickDetector = Bounds:FindFirstChild("PickUp")
                            if ClickDetector and ClickDetector:IsA("ClickDetector") then
                                fireclickdetector(ClickDetector)
                            end
                        end
                    end
                end
            end
        end
    end,
    SectionParent = pinkKeySection
})

local textbookSection = chapter4Tab:CreateSection("Textbook")

chapter4Tab:CreateButton({
    Name = "Grab Textbook",
    Callback = function()
        local CurrentMap = game.Workspace:FindFirstChild("CurrentMap")
        if CurrentMap then
            local Chapter4 = CurrentMap:FindFirstChild("Chapter4")
            if Chapter4 then
                local Utilities = Chapter4:FindFirstChild("Utilities")
                if Utilities then
                    local Textbook = Utilities:FindFirstChild("Textbook")
                    if Textbook then
                        local Bounds = Textbook:FindFirstChild("Bounds")
                        if Bounds then
                            local ClickDetector = Bounds:FindFirstChild("PickUp")
                            if ClickDetector and ClickDetector:IsA("ClickDetector") then
                                fireclickdetector(ClickDetector)
                            end
                        end
                    end
                end
            end
        end
    end,
    SectionParent = textbookSection
})

local whiteKeySection = chapter4Tab:CreateSection("White Key")

chapter4Tab:CreateButton({
    Name = "Grab White Key",
    Callback = function()
        local CurrentMap = game.Workspace:FindFirstChild("CurrentMap")
        if CurrentMap then
            local Chapter4 = CurrentMap:FindFirstChild("Chapter4")
            if Chapter4 then
                local Utilities = Chapter4:FindFirstChild("Utilities")
                if Utilities then
                    local WhiteKey = Utilities:FindFirstChild("White Key")
                    if WhiteKey then
                        local Bounds = WhiteKey:FindFirstChild("Bounds")
                        if Bounds then
                            local ClickDetector = Bounds:FindFirstChild("PickUp")
                            if ClickDetector and ClickDetector:IsA("ClickDetector") then
                                fireclickdetector(ClickDetector)
                            end
                        end
                    end
                end
            end
        end
    end,
    SectionParent = whiteKeySection
})

local taserSection = chapter4Tab:CreateSection("Taser")

chapter4Tab:CreateButton({
    Name = "Grab Taser",
    Callback = function()
        local CurrentMap = game.Workspace:FindFirstChild("CurrentMap")
        if CurrentMap then
            local Chapter4 = CurrentMap:FindFirstChild("Chapter4")
            if Chapter4 then
                local Utilities = Chapter4:FindFirstChild("Utilities")
                if Utilities then
                    local Taser = Utilities:FindFirstChild("Taser")
                    if Taser then
                        local Bounds = Taser:FindFirstChild("Bounds")
                        if Bounds then
                            local ClickDetector = Bounds:FindFirstChild("PickUp")
                            if ClickDetector and ClickDetector:IsA("ClickDetector") then
                                fireclickdetector(ClickDetector)
                            end
                        end
                    end
                end
            end
        end
    end,
    SectionParent = taserSection
})

local chapter5Tab = window:CreateTab("Chapter 5", 4483362458)

local toyHammerSection = chapter5Tab:CreateSection("Toy Hammer")

chapter5Tab:CreateButton({
    Name = "Grab Toy Hammer",
    Callback = function()
        local CurrentMap = game.Workspace:FindFirstChild("CurrentMap")
        if CurrentMap then
            local Chapter5 = CurrentMap:FindFirstChild("Chapter5")
            if Chapter5 then
                local Utilities = Chapter5:FindFirstChild("Utilities")
                if Utilities then
                    local ToyHammer = Utilities:FindFirstChild("Toy Hammer")
                    if ToyHammer then
                        local Bounds = ToyHammer:FindFirstChild("Bounds")
                        if Bounds then
                            local ClickDetector = Bounds:FindFirstChild("PickUp")
                            if ClickDetector and ClickDetector:IsA("ClickDetector") then
                                fireclickdetector(ClickDetector)
                            end
                        end
                    end
                end
            end
        end
    end,
    SectionParent = toyHammerSection
})

local wrenchSection = chapter5Tab:CreateSection("Wrench")

chapter5Tab:CreateButton({
    Name = "Grab Wrench",
    Callback = function()
        local CurrentMap = game.Workspace:FindFirstChild("CurrentMap")
        if CurrentMap then
            local Chapter5 = CurrentMap:FindFirstChild("Chapter5")
            if Chapter5 then
                local Utilities = Chapter5:FindFirstChild("Utilities")
                if Utilities then
                    local Wrench = Utilities:FindFirstChild("Wrench")
                    if Wrench then
                        local Bounds = Wrench:FindFirstChild("Bounds")
                        if Bounds then
                            local ClickDetector = Bounds:FindFirstChild("PickUp")
                            if ClickDetector and ClickDetector:IsA("ClickDetector") then
                                fireclickdetector(ClickDetector)
                            end
                        end
                    end
                end
            end
        end
    end,
    SectionParent = wrenchSection
})

local bearSection = chapter5Tab:CreateSection("Bear")

chapter5Tab:CreateButton({
    Name = "Grab Bear",
    Callback = function()
        local CurrentMap = game.Workspace:FindFirstChild("CurrentMap")
        if CurrentMap then
            local Chapter5 = CurrentMap:FindFirstChild("Chapter5")
            if Chapter5 then
                local Utilities = Chapter5:FindFirstChild("Utilities")
                if Utilities then
                    local Bear = Utilities:FindFirstChild("Bear")
                    if Bear then
                        local Bounds = Bear:FindFirstChild("Bounds")
                        if Bounds then
                            local ClickDetector = Bounds:FindFirstChild("PickUp")
                            if ClickDetector and ClickDetector:IsA("ClickDetector") then
                                fireclickdetector(ClickDetector)
                            end
                        end
                    end
                end
            end
        end
    end,
    SectionParent = bearSection
})

local blueKeySection = chapter5Tab:CreateSection("Blue Key")

chapter5Tab:CreateButton({
    Name = "Grab Blue Key",
    Callback = function()
        local CurrentMap = game.Workspace:FindFirstChild("CurrentMap")
        if CurrentMap then
            local Chapter5 = CurrentMap:FindFirstChild("Chapter5")
            if Chapter5 then
                local Utilities = Chapter5:FindFirstChild("Utilities")
                if Utilities then
                    local BlueKey = Utilities:FindFirstChild("Blue Key")
                    if BlueKey then
                        local Bounds = BlueKey:FindFirstChild("Bounds")
                        if Bounds then
                            local ClickDetector = Bounds:FindFirstChild("PickUp")
                            if ClickDetector and ClickDetector:IsA("ClickDetector") then
                                fireclickdetector(ClickDetector)
                            end
                        end
                    end
                end
            end
        end
    end,
    SectionParent = blueKeySection
})

local knobSection = chapter5Tab:CreateSection("Knob")

chapter5Tab:CreateButton({
    Name = "Grab Knob",
    Callback = function()
        local CurrentMap = game.Workspace:FindFirstChild("CurrentMap")
        if CurrentMap then
            local Chapter5 = CurrentMap:FindFirstChild("Chapter5")
            if Chapter5 then
                local Utilities = Chapter5:FindFirstChild("Utilities")
                if Utilities then
                    local Knob = Utilities:FindFirstChild("Knob")
                    if Knob then
                        local Bounds = Knob:FindFirstChild("Bounds")
                        if Bounds then
                            local ClickDetector = Bounds:FindFirstChild("PickUp")
                            if ClickDetector and ClickDetector:IsA("ClickDetector") then
                                fireclickdetector(ClickDetector)
                            end
                        end
                    end
                end
            end
        end
    end,
    SectionParent = knobSection
})

local whiteKeySection = chapter5Tab:CreateSection("White Key")

chapter5Tab:CreateButton({
    Name = "Grab White Key",
    Callback = function()
        local CurrentMap = game.Workspace:FindFirstChild("CurrentMap")
        if CurrentMap then
            local Chapter5 = CurrentMap:FindFirstChild("Chapter5")
            if Chapter5 then
                local Utilities = Chapter5:FindFirstChild("Utilities")
                if Utilities then
                    local WhiteKey = Utilities:FindFirstChild("White Key")
                    if WhiteKey then
                        local Bounds = WhiteKey:FindFirstChild("Bounds")
                        if Bounds then
                            local ClickDetector = Bounds:FindFirstChild("PickUp")
                            if ClickDetector and ClickDetector:IsA("ClickDetector") then
                                fireclickdetector(ClickDetector)
                            end
                        end
                    end
                end
            end
        end
    end,
    SectionParent = whiteKeySection
})

local taserSection = chapter5Tab:CreateSection("Taser")

chapter5Tab:CreateButton({
    Name = "Grab Taser",
    Callback = function()
        local CurrentMap = game.Workspace:FindFirstChild("CurrentMap")
        if CurrentMap then
            local Chapter5 = CurrentMap:FindFirstChild("Chapter5")
            if Chapter5 then
                local Utilities = Chapter5:FindFirstChild("Utilities")
                if Utilities then
                    local Taser = Utilities:FindFirstChild("Taser")
                    if Taser then
                        local Bounds = Taser:FindFirstChild("Bounds")
                        if Bounds then
                            local ClickDetector = Bounds:FindFirstChild("PickUp")
                            if ClickDetector and ClickDetector:IsA("ClickDetector") then
                                fireclickdetector(ClickDetector)
                            end
                        end
                    end
                end
            end
        end
    end,
    SectionParent = taserSection
})

local chapter6Tab = window:CreateTab("Chapter 6", 4483362458)

local blueKeySection = chapter6Tab:CreateSection("Blue Key")

chapter6Tab:CreateButton({
    Name = "Grab Blue Key",
    Callback = function()
        local CurrentMap = game.Workspace:FindFirstChild("CurrentMap")
        if CurrentMap then
            local Chapter6 = CurrentMap:FindFirstChild("Chapter6")
            if Chapter6 then
                local Utilities = Chapter6:FindFirstChild("Utilities")
                if Utilities then
                    local BlueKey = Utilities:FindFirstChild("Blue Key")
                    if BlueKey then
                        local Bounds = BlueKey:FindFirstChild("Bounds")
                        if Bounds then
                            local ClickDetector = Bounds:FindFirstChild("PickUp")
                            if ClickDetector and ClickDetector:IsA("ClickDetector") then
                                fireclickdetector(ClickDetector)
                            end
                        end
                    end
                end
            end
        end
    end,
    SectionParent = blueKeySection
})

local idCardSection = chapter6Tab:CreateSection("ID Card")

chapter6Tab:CreateButton({
    Name = "Grab ID Card",
    Callback = function()
        local CurrentMap = game.Workspace:FindFirstChild("CurrentMap")
        if CurrentMap then
            local Chapter6 = CurrentMap:FindFirstChild("Chapter6")
            if Chapter6 then
                local Utilities = Chapter6:FindFirstChild("Utilities")
                if Utilities then
                    local IDCard = Utilities:FindFirstChild("ID Card")
                    if IDCard then
                        local Bounds = IDCard:FindFirstChild("Bounds")
                        if Bounds then
                            local ClickDetector = Bounds:FindFirstChild("PickUp")
                            if ClickDetector and ClickDetector:IsA("ClickDetector") then
                                fireclickdetector(ClickDetector)
                            end
                        end
                    end
                end
            end
        end
    end,
    SectionParent = idCardSection
})

local crowbarSection = chapter6Tab:CreateSection("Crowbar")

chapter6Tab:CreateButton({
    Name = "Grab Crowbar",
    Callback = function()
        local CurrentMap = game.Workspace:FindFirstChild("CurrentMap")
        if CurrentMap then
            local Chapter6 = CurrentMap:FindFirstChild("Chapter6")
            if Chapter6 then
                local Utilities = Chapter6:FindFirstChild("Utilities")
                if Utilities then
                    local Crowbar = Utilities:FindFirstChild("Crowbar")
                    if Crowbar then
                        local Bounds = Crowbar:FindFirstChild("Bounds")
                        if Bounds then
                            local ClickDetector = Bounds:FindFirstChild("PickUp")
                            if ClickDetector and ClickDetector:IsA("ClickDetector") then
                                fireclickdetector(ClickDetector)
                            end
                        end
                    end
                end
            end
        end
    end,
    SectionParent = crowbarSection
})

local wrenchSection = chapter6Tab:CreateSection("Wrench")

chapter6Tab:CreateButton({
    Name = "Grab Wrench",
    Callback = function()
        local CurrentMap = game.Workspace:FindFirstChild("CurrentMap")
        if CurrentMap then
            local Chapter6 = CurrentMap:FindFirstChild("Chapter6")
            if Chapter6 then
                local Utilities = Chapter6:FindFirstChild("Utilities")
                if Utilities then
                    local Wrench = Utilities:FindFirstChild("Wrench")
                    if Wrench then
                        local Bounds = Wrench:FindFirstChild("Bounds")
                        if Bounds then
                            local ClickDetector = Bounds:FindFirstChild("PickUp")
                            if ClickDetector and ClickDetector:IsA("ClickDetector") then
                                fireclickdetector(ClickDetector)
                            end
                        end
                    end
                end
            end
        end
    end,
    SectionParent = wrenchSection
})

local batterySection = chapter6Tab:CreateSection("Battery")

chapter6Tab:CreateButton({
    Name = "Grab Battery",
    Callback = function()
        local CurrentMap = game.Workspace:FindFirstChild("CurrentMap")
        if CurrentMap then
            local Chapter6 = CurrentMap:FindFirstChild("Chapter6")
            if Chapter6 then
                local Utilities = Chapter6:FindFirstChild("Utilities")
                if Utilities then
                    local Battery = Utilities:FindFirstChild("Battery")
                    if Battery then
                        local Bounds = Battery:FindFirstChild("Bounds")
                        if Bounds then
                            local ClickDetector = Bounds:FindFirstChild("PickUp")
                            if ClickDetector and ClickDetector:IsA("ClickDetector") then
                                fireclickdetector(ClickDetector)
                            end
                        end
                    end
                end
            end
        end
    end,
    SectionParent = batterySection
})

local whiteKeySection = chapter6Tab:CreateSection("White Key")

chapter6Tab:CreateButton({
    Name = "Grab White Key",
    Callback = function()
        local CurrentMap = game.Workspace:FindFirstChild("CurrentMap")
        if CurrentMap then
            local Chapter6 = CurrentMap:FindFirstChild("Chapter6")
            if Chapter6 then
                local Utilities = Chapter6:FindFirstChild("Utilities")
                if Utilities then
                    local WhiteKey = Utilities:FindFirstChild("White Key")
                    if WhiteKey then
                        local Bounds = WhiteKey:FindFirstChild("Bounds")
                        if Bounds then
                            local ClickDetector = Bounds:FindFirstChild("PickUp")
                            if ClickDetector and ClickDetector:IsA("ClickDetector") then
                                fireclickdetector(ClickDetector)
                            end
                        end
                    end
                end
            end
        end
    end,
    SectionParent = whiteKeySection
})

local taserSection = chapter6Tab:CreateSection("Taser")

chapter6Tab:CreateButton({
    Name = "Grab Taser",
    Callback = function()
        local CurrentMap = game.Workspace:FindFirstChild("CurrentMap")
        if CurrentMap then
            local Chapter6 = CurrentMap:FindFirstChild("Chapter6")
            if Chapter6 then
                local Utilities = Chapter6:FindFirstChild("Utilities")
                if Utilities then
                    local Taser = Utilities:FindFirstChild("Taser")
                    if Taser then
                        local Bounds = Taser:FindFirstChild("Bounds")
                        if Bounds then
                            local ClickDetector = Bounds:FindFirstChild("PickUp")
                            if ClickDetector and ClickDetector:IsA("ClickDetector") then
                                fireclickdetector(ClickDetector)
                            end
                        end
                    end
                end
            end
        end
    end,
    SectionParent = taserSection
})

local chapter7Tab = window:CreateTab("Chapter 7", 4483362458)

local redKeySection = chapter7Tab:CreateSection("Red Key")

chapter7Tab:CreateButton({
    Name = "Grab Red Key",
    Callback = function()
        local CurrentMap = game.Workspace:FindFirstChild("CurrentMap")
        if CurrentMap then
            local Chapter7 = CurrentMap:FindFirstChild("Chapter7")
            if Chapter7 then
                local Utilities = Chapter7:FindFirstChild("Utilities")
                if Utilities then
                    local RedKey = Utilities:FindFirstChild("Red Key")
                    if RedKey then
                        local Bounds = RedKey:FindFirstChild("Bounds")
                        if Bounds then
                            local ClickDetector = Bounds:FindFirstChild("PickUp")
                            if ClickDetector and ClickDetector:IsA("ClickDetector") then
                                fireclickdetector(ClickDetector)
                            end
                        end
                    end
                end
            end
        end
    end,
    SectionParent = redKeySection
})

local idCardSection = chapter7Tab:CreateSection("ID Card")

chapter7Tab:CreateButton({
    Name = "Grab ID Card",
    Callback = function()
        local CurrentMap = game.Workspace:FindFirstChild("CurrentMap")
        if CurrentMap then
            local Chapter7 = CurrentMap:FindFirstChild("Chapter7")
            if Chapter7 then
                local Utilities = Chapter7:FindFirstChild("Utilities")
                if Utilities then
                    local IDCard = Utilities:FindFirstChild("ID Card")
                    if IDCard then
                        local Bounds = IDCard:FindFirstChild("Bounds")
                        if Bounds then
                            local ClickDetector = Bounds:FindFirstChild("PickUp")
                            if ClickDetector and ClickDetector:IsA("ClickDetector") then
                                fireclickdetector(ClickDetector)
                            end
                        end
                    end
                end
            end
        end
    end,
    SectionParent = idCardSection
})

local leverSection = chapter7Tab:CreateSection("Lever")

chapter7Tab:CreateButton({
    Name = "Grab Lever",
    Callback = function()
        local CurrentMap = game.Workspace:FindFirstChild("CurrentMap")
        if CurrentMap then
            local Chapter7 = CurrentMap:FindFirstChild("Chapter7")
            if Chapter7 then
                local Utilities = Chapter7:FindFirstChild("Utilities")
                if Utilities then
                    local Lever = Utilities:FindFirstChild("Lever")
                    if Lever then
                        local Bounds = Lever:FindFirstChild("Bounds")
                        if Bounds then
                            local ClickDetector = Bounds:FindFirstChild("PickUp")
                            if ClickDetector and ClickDetector:IsA("ClickDetector") then
                                fireclickdetector(ClickDetector)
                            end
                        end
                    end
                end
            end
        end
    end,
    SectionParent = leverSection
})

local shovelSection = chapter7Tab:CreateSection("Shovel")

chapter7Tab:CreateButton({
    Name = "Grab Shovel",
    Callback = function()
        local CurrentMap = game.Workspace:FindFirstChild("CurrentMap")
        if CurrentMap then
            local Chapter7 = CurrentMap:FindFirstChild("Chapter7")
            if Chapter7 then
                local Utilities = Chapter7:FindFirstChild("Utilities")
                if Utilities then
                    local Shovel = Utilities:FindFirstChild("Shovel")
                    if Shovel then
                        local Bounds = Shovel:FindFirstChild("Bounds")
                        if Bounds then
                            local ClickDetector = Bounds:FindFirstChild("PickUp")
                            if ClickDetector and ClickDetector:IsA("ClickDetector") then
                                fireclickdetector(ClickDetector)
                            end
                        end
                    end
                end
            end
        end
    end,
    SectionParent = shovelSection
})

local redButtonSection = chapter7Tab:CreateSection("Red Button")

chapter7Tab:CreateButton({
    Name = "Grab Red Button",
    Callback = function()
        local CurrentMap = game.Workspace:FindFirstChild("CurrentMap")
        if CurrentMap then
            local Chapter7 = CurrentMap:FindFirstChild("Chapter7")
            if Chapter7 then
                local Utilities = Chapter7:FindFirstChild("Utilities")
                if Utilities then
                    local RedButton = Utilities:FindFirstChild("Red Button")
                    if RedButton then
                        local Bounds = RedButton:FindFirstChild("Bounds")
                        if Bounds then
                            local ClickDetector = Bounds:FindFirstChild("PickUp")
                            if ClickDetector and ClickDetector:IsA("ClickDetector") then
                                fireclickdetector(ClickDetector)
                            end
                        end
                    end
                end
            end
        end
    end,
    SectionParent = redButtonSection
})

local whiteKeySection = chapter7Tab:CreateSection("White Key")

chapter7Tab:CreateButton({
    Name = "Grab White Key",
    Callback = function()
        local CurrentMap = game.Workspace:FindFirstChild("CurrentMap")
        if CurrentMap then
            local Chapter7 = CurrentMap:FindFirstChild("Chapter7")
            if Chapter7 then
                local Utilities = Chapter7:FindFirstChild("Utilities")
                if Utilities then
                    local WhiteKey = Utilities:FindFirstChild("White Key")
                    if WhiteKey then
                        local Bounds = WhiteKey:FindFirstChild("Bounds")
                        if Bounds then
                            local ClickDetector = Bounds:FindFirstChild("PickUp")
                            if ClickDetector and ClickDetector:IsA("ClickDetector") then
                                fireclickdetector(ClickDetector)
                            end
                        end
                    end
                end
            end
        end
    end,
    SectionParent = whiteKeySection
})

local taserSection = chapter7Tab:CreateSection("Taser")

chapter7Tab:CreateButton({
    Name = "Grab Taser",
    Callback = function()
        local CurrentMap = game.Workspace:FindFirstChild("CurrentMap")
        if CurrentMap then
            local Chapter7 = CurrentMap:FindFirstChild("Chapter7")
            if Chapter7 then
                local Utilities = Chapter7:FindFirstChild("Utilities")
                if Utilities then
                    local Taser = Utilities:FindFirstChild("Taser")
                    if Taser then
                        local Bounds = Taser:FindFirstChild("Bounds")
                        if Bounds then
                            local ClickDetector = Bounds:FindFirstChild("PickUp")
                            if ClickDetector and ClickDetector:IsA("ClickDetector") then
                                fireclickdetector(ClickDetector)
                            end
                        end
                    end
                end
            end
        end
    end,
    SectionParent = taserSection
})

local chapter8Tab = window:CreateTab("Chapter 8", 4483362458)

local greenKeySection = chapter8Tab:CreateSection("Green Key")

chapter8Tab:CreateButton({
    Name = "Grab Green Key",
    Callback = function()
        local CurrentMap = game.Workspace:FindFirstChild("CurrentMap")
        if CurrentMap then
            local Chapter8 = CurrentMap:FindFirstChild("Chapter8")
            if Chapter8 then
                local Utilities = Chapter8:FindFirstChild("Utilities")
                if Utilities then
                    local GreenKey = Utilities:FindFirstChild("Green Key")
                    if GreenKey then
                        local Bounds = GreenKey:FindFirstChild("Bounds")
                        if Bounds then
                            local ClickDetector = Bounds:FindFirstChild("PickUp")
                            if ClickDetector and ClickDetector:IsA("ClickDetector") then
                                fireclickdetector(ClickDetector)
                            end
                        end
                    end
                end
            end
        end
    end,
    SectionParent = greenKeySection
})

local truckSection = chapter8Tab:CreateSection("Truck")

chapter8Tab:CreateButton({
    Name = "Grab Truck",
    Callback = function()
        local CurrentMap = game.Workspace:FindFirstChild("CurrentMap")
        if CurrentMap then
            local Chapter8 = CurrentMap:FindFirstChild("Chapter8")
            if Chapter8 then
                local Utilities = Chapter8:FindFirstChild("Utilities")
                if Utilities then
                    local Truck = Utilities:FindFirstChild("Truck")
                    if Truck then
                        local Bounds = Truck:FindFirstChild("Bounds")
                        if Bounds then
                            local ClickDetector = Bounds:FindFirstChild("PickUp")
                            if ClickDetector and ClickDetector:IsA("ClickDetector") then
                                fireclickdetector(ClickDetector)
                            end
                        end
                    end
                end
            end
        end
    end,
    SectionParent = truckSection
})

local teddySection = chapter8Tab:CreateSection("Teddy")

chapter8Tab:CreateButton({
    Name = "Grab Teddy",
    Callback = function()
        local CurrentMap = game.Workspace:FindFirstChild("CurrentMap")
        if CurrentMap then
            local Chapter8 = CurrentMap:FindFirstChild("Chapter8")
            if Chapter8 then
                local Utilities = Chapter8:FindFirstChild("Utilities")
                if Utilities then
                    local Teddy = Utilities:FindFirstChild("Teddy")
                    if Teddy then
                        local Bounds = Teddy:FindFirstChild("Bounds")
                        if Bounds then
                            local ClickDetector = Bounds:FindFirstChild("PickUp")
                            if ClickDetector and ClickDetector:IsA("ClickDetector") then
                                fireclickdetector(ClickDetector)
                            end
                        end
                    end
                end
            end
        end
    end,
    SectionParent = teddySection
})

local whiteKeySection = chapter8Tab:CreateSection("White Key")

chapter8Tab:CreateButton({
    Name = "Grab White Key",
    Callback = function()
        local CurrentMap = game.Workspace:FindFirstChild("CurrentMap")
        if CurrentMap then
            local Chapter8 = CurrentMap:FindFirstChild("Chapter8")
            if Chapter8 then
                local Utilities = Chapter8:FindFirstChild("Utilities")
                if Utilities then
                    local WhiteKey = Utilities:FindFirstChild("White Key")
                    if WhiteKey then
                        local Bounds = WhiteKey:FindFirstChild("Bounds")
                        if Bounds then
                            local ClickDetector = Bounds:FindFirstChild("PickUp")
                            if ClickDetector and ClickDetector:IsA("ClickDetector") then
                                fireclickdetector(ClickDetector)
                            end
                        end
                    end
                end
            end
        end
    end,
    SectionParent = whiteKeySection
})

local taserSection = chapter8Tab:CreateSection("Taser")

chapter8Tab:CreateButton({
    Name = "Grab Taser",
    Callback = function()
        local CurrentMap = game.Workspace:FindFirstChild("CurrentMap")
        if CurrentMap then
            local Chapter8 = CurrentMap:FindFirstChild("Chapter8")
            if Chapter8 then
                local Utilities = Chapter8:FindFirstChild("Utilities")
                if Utilities then
                    local Taser = Utilities:FindFirstChild("Taser")
                    if Taser then
                        local Bounds = Taser:FindFirstChild("Bounds")
                        if Bounds then
                            local ClickDetector = Bounds:FindFirstChild("PickUp")
                            if ClickDetector and ClickDetector:IsA("ClickDetector") then
                                fireclickdetector(ClickDetector)
                            end
                        end
                    end
                end
            end
        end
    end,
    SectionParent = taserSection
})

local chapter9Tab = window:CreateTab("Chapter 9", 4483362458)

local keysSection = chapter9Tab:CreateSection("Keys")

chapter9Tab:CreateButton({
    Name = "Grab Red Key",
    Callback = function()
        local CurrentMap = game.Workspace:FindFirstChild("CurrentMap")
        if CurrentMap then
            local Chapter9 = CurrentMap:FindFirstChild("Chapter9")
            if Chapter9 then
                local Utilities = Chapter9:FindFirstChild("Utilities")
                if Utilities then
                    local RedKey = Utilities:FindFirstChild("Red Key")
                    if RedKey then
                        local Bounds = RedKey:FindFirstChild("Bounds")
                        if Bounds then
                            local ClickDetector = Bounds:FindFirstChild("PickUp")
                            if ClickDetector and ClickDetector:IsA("ClickDetector") then
                                fireclickdetector(ClickDetector)
                            end
                        end
                    end
                end
            end
        end
    end,
    SectionParent = keysSection
})

chapter9Tab:CreateButton({
    Name = "Grab Green Key",
    Callback = function()
        local CurrentMap = game.Workspace:FindFirstChild("CurrentMap")
        if CurrentMap then
            local Chapter9 = CurrentMap:FindFirstChild("Chapter9")
            if Chapter9 then
                local Utilities = Chapter9:FindFirstChild("Utilities")
                if Utilities then
                    local GreenKey = Utilities:FindFirstChild("Green Key")
                    if GreenKey then
                        local Bounds = GreenKey:FindFirstChild("Bounds")
                        if Bounds then
                            local ClickDetector = Bounds:FindFirstChild("PickUp")
                            if ClickDetector and ClickDetector:IsA("ClickDetector") then
                                fireclickdetector(ClickDetector)
                            end
                        end
                    end
                end
            end
        end
    end,
    SectionParent = keysSection
})

chapter9Tab:CreateButton({
    Name = "Grab Blue Key",
    Callback = function()
        local CurrentMap = game.Workspace:FindFirstChild("CurrentMap")
        if CurrentMap then
            local Chapter9 = CurrentMap:FindFirstChild("Chapter9")
            if Chapter9 then
                local Utilities = Chapter9:FindFirstChild("Utilities")
                if Utilities then
                    local BlueKey = Utilities:FindFirstChild("Blue Key")
                    if BlueKey then
                        local Bounds = BlueKey:FindFirstChild("Bounds")
                        if Bounds then
                            local ClickDetector = Bounds:FindFirstChild("PickUp")
                            if ClickDetector and ClickDetector:IsA("ClickDetector") then
                                fireclickdetector(ClickDetector)
                            end
                        end
                    end
                end
            end
        end
    end,
    SectionParent = keysSection
})

chapter9Tab:CreateButton({
    Name = "Grab Orange Key",
    Callback = function()
        local CurrentMap = game.Workspace:FindFirstChild("CurrentMap")
        if CurrentMap then
            local Chapter9 = CurrentMap:FindFirstChild("Chapter9")
            if Chapter9 then
                local Utilities = Chapter9:FindFirstChild("Utilities")
                if Utilities then
                    local OrangeKey = Utilities:FindFirstChild("Orange Key")
                    if OrangeKey then
                        local Bounds = OrangeKey:FindFirstChild("Bounds")
                        if Bounds then
                            local ClickDetector = Bounds:FindFirstChild("PickUp")
                            if ClickDetector and ClickDetector:IsA("ClickDetector") then
                                fireclickdetector(ClickDetector)
                            end
                        end
                    end
                end
            end
        end
    end,
    SectionParent = keysSection
})

local rodSection = chapter9Tab:CreateSection("Rod")

chapter9Tab:CreateButton({
    Name = "Grab Rod",
    Callback = function()
        local CurrentMap = game.Workspace:FindFirstChild("CurrentMap")
        if CurrentMap then
            local Chapter9 = CurrentMap:FindFirstChild("Chapter9")
            if Chapter9 then
                local Utilities = Chapter9:FindFirstChild("Utilities")
                if Utilities then
                    local Rod = Utilities:FindFirstChild("Rod")
                    if Rod then
                        local Bounds = Rod:FindFirstChild("Bounds")
                        if Bounds then
                            local ClickDetector = Bounds:FindFirstChild("PickUp")
                            if ClickDetector and ClickDetector:IsA("ClickDetector") then
                                fireclickdetector(ClickDetector)
                            end
                        end
                    end
                end
            end
        end
    end,
    SectionParent = rodSection
})

local fishSection = chapter9Tab:CreateSection("Fish")

chapter9Tab:CreateButton({
    Name = "Grab Fish",
    Callback = function()
        local CurrentMap = game.Workspace:FindFirstChild("CurrentMap")
        if CurrentMap then
            local Chapter9 = CurrentMap:FindFirstChild("Chapter9")
            if Chapter9 then
                local Utilities = Chapter9:FindFirstChild("Utilities")
                if Utilities then
                    local Fish = Utilities:FindFirstChild("Fish")
                    if Fish then
                        local Bounds = Fish:FindFirstChild("Bounds")
                        if Bounds then
                            local ClickDetector = Bounds:FindFirstChild("PickUp")
                            if ClickDetector and ClickDetector:IsA("ClickDetector") then
                                fireclickdetector(ClickDetector)
                            end
                        end
                    end
                end
            end
        end
    end,
    SectionParent = fishSection
})

local axeSection = chapter9Tab:CreateSection("Axe")

chapter9Tab:CreateButton({
    Name = "Grab Axe",
    Callback = function()
        local CurrentMap = game.Workspace:FindFirstChild("CurrentMap")
        if CurrentMap then
            local Chapter9 = CurrentMap:FindFirstChild("Chapter9")
            if Chapter9 then
                local Utilities = Chapter9:FindFirstChild("Utilities")
                if Utilities then
                    local Axe = Utilities:FindFirstChild("Ax")
                    if Axe then
                        local Bounds = Axe:FindFirstChild("Bounds")
                        if Bounds then
                            local ClickDetector = Bounds:FindFirstChild("PickUp")
                            if ClickDetector and ClickDetector:IsA("ClickDetector") then
                                fireclickdetector(ClickDetector)
                            end
                        end
                    end
                end
            end
        end
    end,
    SectionParent = axeSection
})

local hammerSection = chapter9Tab:CreateSection("Hammer")

chapter9Tab:CreateButton({
    Name = "Grab Hammer",
    Callback = function()
        local CurrentMap = game.Workspace:FindFirstChild("CurrentMap")
        if CurrentMap then
            local Chapter9 = CurrentMap:FindFirstChild("Chapter9")
            if Chapter9 then
                local Utilities = Chapter9:FindFirstChild("Utilities")
                if Utilities then
                    local Hammer = Utilities:FindFirstChild("Hammer")
                    if Hammer then
                        local Bounds = Hammer:FindFirstChild("Bounds")
                        if Bounds then
                            local ClickDetector = Bounds:FindFirstChild("PickUp")
                            if ClickDetector and ClickDetector:IsA("ClickDetector") then
                                fireclickdetector(ClickDetector)
                            end
                        end
                    end
                end
            end
        end
    end,
    SectionParent = hammerSection
})

local shovelSection = chapter9Tab:CreateSection("Shovel")

chapter9Tab:CreateButton({
    Name = "Grab Shovel",
    Callback = function()
        local CurrentMap = game.Workspace:FindFirstChild("CurrentMap")
        if CurrentMap then
            local Chapter9 = CurrentMap:FindFirstChild("Chapter9")
            if Chapter9 then
                local Utilities = Chapter9:FindFirstChild("Utilities")
                if Utilities then
                    local Shovel = Utilities:FindFirstChild("Shovel")
                    if Shovel then
                        local Bounds = Shovel:FindFirstChild("Bounds")
                        if Bounds then
                            local ClickDetector = Bounds:FindFirstChild("PickUp")
                            if ClickDetector and ClickDetector:IsA("ClickDetector") then
                                fireclickdetector(ClickDetector)
                            end
                        end
                    end
                end
            end
        end
    end,
    SectionParent = shovelSection
})

local whiteKeySection = chapter9Tab:CreateSection("White Key")

chapter9Tab:CreateButton({
    Name = "Grab White Key",
    Callback = function()
        local CurrentMap = game.Workspace:FindFirstChild("CurrentMap")
        if CurrentMap then
            local Chapter9 = CurrentMap:FindFirstChild("Chapter9")
            if Chapter9 then
                local Utilities = Chapter9:FindFirstChild("Utilities")
                if Utilities then
                    local WhiteKey = Utilities:FindFirstChild("White Key")
                    if WhiteKey then
                        local Bounds = WhiteKey:FindFirstChild("Bounds")
                        if Bounds then
                            local ClickDetector = Bounds:FindFirstChild("PickUp")
                            if ClickDetector and ClickDetector:IsA("ClickDetector") then
                                fireclickdetector(ClickDetector)
                            end
                        end
                    end
                end
            end
        end
    end,
    SectionParent = whiteKeySection
})

local chapter10Tab = window:CreateTab("Chapter 10", 4483362458)

local keysSection = chapter10Tab:CreateSection("Keys")

chapter10Tab:CreateButton({
    Name = "Grab Blue Key",
    Callback = function()
        local CurrentMap = game.Workspace:FindFirstChild("CurrentMap")
        if CurrentMap then
            local Chapter10 = CurrentMap:FindFirstChild("Chapter10")
            if Chapter10 then
                local Utilities = Chapter10:FindFirstChild("Utilities")
                if Utilities then
                    local BlueKey = Utilities:FindFirstChild("Blue Key")
                    if BlueKey then
                        local Bounds = BlueKey:FindFirstChild("Bounds")
                        if Bounds then
                            local ClickDetector = Bounds:FindFirstChild("PickUp")
                            if ClickDetector and ClickDetector:IsA("ClickDetector") then
                                fireclickdetector(ClickDetector)
                            end
                        end
                    end
                end
            end
        end
    end,
    SectionParent = keysSection
})

chapter10Tab:CreateButton({
    Name = "Grab Orange Key",
    Callback = function()
        local CurrentMap = game.Workspace:FindFirstChild("CurrentMap")
        if CurrentMap then
            local Chapter10 = CurrentMap:FindFirstChild("Chapter10")
            if Chapter10 then
                local Utilities = Chapter10:FindFirstChild("Utilities")
                if Utilities then
                    local OrangeKey = Utilities:FindFirstChild("Orange Key")
                    if OrangeKey then
                        local Bounds = OrangeKey:FindFirstChild("Bounds")
                        if Bounds then
                            local ClickDetector = Bounds:FindFirstChild("PickUp")
                            if ClickDetector and ClickDetector:IsA("ClickDetector") then
                                fireclickdetector(ClickDetector)
                            end
                        end
                    end
                end
            end
        end
    end,
    SectionParent = keysSection
})

chapter10Tab:CreateButton({
    Name = "Grab Pink Key",
    Callback = function()
        local CurrentMap = game.Workspace:FindFirstChild("CurrentMap")
        if CurrentMap then
            local Chapter10 = CurrentMap:FindFirstChild("Chapter10")
            if Chapter10 then
                local Utilities = Chapter10:FindFirstChild("Utilities")
                if Utilities then
                    local PinkKey = Utilities:FindFirstChild("Pink Key")
                    if PinkKey then
                        local Bounds = PinkKey:FindFirstChild("Bounds")
                        if Bounds then
                            local ClickDetector = Bounds:FindFirstChild("PickUp")
                            if ClickDetector and ClickDetector:IsA("ClickDetector") then
                                fireclickdetector(ClickDetector)
                            end
                        end
                    end
                end
            end
        end
    end,
    SectionParent = keysSection
})

chapter10Tab:CreateButton({
    Name = "Grab Red Key",
    Callback = function()
        local CurrentMap = game.Workspace:FindFirstChild("CurrentMap")
        if CurrentMap then
            local Chapter10 = CurrentMap:FindFirstChild("Chapter10")
            if Chapter10 then
                local Utilities = Chapter10:FindFirstChild("Utilities")
                if Utilities then
                    local RedKey = Utilities:FindFirstChild("Red Key")
                    if RedKey then
                        local Bounds = RedKey:FindFirstChild("Bounds")
                        if Bounds then
                            local ClickDetector = Bounds:FindFirstChild("PickUp")
                            if ClickDetector and ClickDetector:IsA("ClickDetector") then
                                fireclickdetector(ClickDetector)
                            end
                        end
                    end
                end
            end
        end
    end,
    SectionParent = keysSection
})

local crowbarSection = chapter10Tab:CreateSection("Crowbar")

chapter10Tab:CreateButton({
    Name = "Grab Crowbar",
    Callback = function()
        local CurrentMap = game.Workspace:FindFirstChild("CurrentMap")
        if CurrentMap then
            local Chapter10 = CurrentMap:FindFirstChild("Chapter10")
            if Chapter10 then
                local Utilities = Chapter10:FindFirstChild("Utilities")
                if Utilities then
                    local Crowbar = Utilities:FindFirstChild("Crowbar")
                    if Crowbar then
                        local Bounds = Crowbar:FindFirstChild("Bounds")
                        if Bounds then
                            local ClickDetector = Bounds:FindFirstChild("PickUp")
                            if ClickDetector and ClickDetector:IsA("ClickDetector") then
                                fireclickdetector(ClickDetector)
                            end
                        end
                    end
                end
            end
        end
    end,
    SectionParent = crowbarSection
})

local goldenGearSection = chapter10Tab:CreateSection("Golden Gear")

chapter10Tab:CreateButton({
    Name = "Grab Golden Gear",
    Callback = function()
        local CurrentMap = game.Workspace:FindFirstChild("CurrentMap")
        if CurrentMap then
            local Chapter10 = CurrentMap:FindFirstChild("Chapter10")
            if Chapter10 then
                local Utilities = Chapter10:FindFirstChild("Utilities")
                if Utilities then
                    local GoldenGear = Utilities:FindFirstChild("Golden Gear")
                    if GoldenGear then
                        local Bounds = GoldenGear:FindFirstChild("Bounds")
                        if Bounds then
                            local ClickDetector = Bounds:FindFirstChild("PickUp")
                            if ClickDetector and ClickDetector:IsA("ClickDetector") then
                                fireclickdetector(ClickDetector)
                            end
                        end
                    end
                end
            end
        end
    end,
    SectionParent = goldenGearSection
})

Rayfield:LoadConfiguration()
