local Rayfield = loadstring(game:HttpGet('https://sirius.menu/rayfield'))()

local window = Rayfield:CreateWindow({
    Name = "You don't wanna see me infuriated - Alpha Hax",
    LoadingTitle = "Loading Assets..",
    LoadingSubtitle = "Created By Derpixh",
    ConfigurationSaving = {
        Enabled = false,
        FileName = "Sirius"
    },
    KeySystem = true,
    KeySettings = {
        Title = "Derpixh's Key System",
        Subtitle = "Enter Your Key",
        Note = "Rate The Features",
        FileName = "Sirius",
        SaveKey = false,
        Key = "2025"
    }
})

local stage1Tab = window:CreateTab("Stage 1")

local obbySection = stage1Tab:CreateSection("Obby")

local freezeObbyButton = stage1Tab:CreateButton({
    Name = "Freeze Obby (Easier To Walk Across)",
    SectionParent = obbySection,
    Callback = function()
        local stageParts = workspace:FindFirstChild("StageParts")
        if stageParts then
            local folder1 = stageParts:FindFirstChild("1")
            if folder1 then
                local reallyGoodBridge = folder1:FindFirstChild("ReallyGoodBridge")
                if reallyGoodBridge then
                    local bridgeParts = reallyGoodBridge:FindFirstChild("BridgeParts")
                    if bridgeParts then
                        for _, part in ipairs(bridgeParts:GetChildren()) do
                            if part.Name == "Baseplate" and part:IsA("Part") then
                                part.Anchored = true
                            end
                        end
                    end
                end
            end
        end
    end
})

local miscStage1Section = stage1Tab:CreateSection("Misc")

local winObbyButtonStage1 = stage1Tab:CreateButton({
    Name = "Win Obby (Shortcut For Lazy Players)",
    SectionParent = miscStage1Section,
    Callback = function()
        local obbyStages = workspace:FindFirstChild("ObbyStages")
        if obbyStages then
            local part1 = obbyStages:FindFirstChild("1")
            if part1 and part1:IsA("Part") then
                local touchInterest = part1:FindFirstChild("TouchInterest")
                if touchInterest then
                    firetouchinterest(game.Players.LocalPlayer.Character.HumanoidRootPart, part1, 0)
                    firetouchinterest(game.Players.LocalPlayer.Character.HumanoidRootPart, part1, 1)
                end
                game.Players.LocalPlayer.Character.HumanoidRootPart.CFrame = part1.CFrame
            end
        end
    end
})

local stage2Tab = window:CreateTab("Stage 2")

local quizDoorsSection = stage2Tab:CreateSection("Quiz Doors")

local openQuizDoorsButton = stage2Tab:CreateButton({
    Name = "Open Quiz Doors (You Can Walk Through The Doors)",
    SectionParent = quizDoorsSection,
    Callback = function()
        local stageParts = workspace:FindFirstChild("StageParts")
        if stageParts then
            local folder2 = stageParts:FindFirstChild("2")
            if folder2 then
                local doorsFolder = folder2:FindFirstChild("Doors")
                if doorsFolder then
                    for _, part in ipairs(doorsFolder:GetChildren()) do
                        if part.Name == "Quiz Door" and part:IsA("Part") then
                            part.Transparency = 1
                            part.CanCollide = false
                        end
                    end
                end
            end
        end
    end
})

local miscStage2Section = stage2Tab:CreateSection("Misc")

local winObbyButtonStage2 = stage2Tab:CreateButton({
    Name = "Win Obby (For Those Who Want A Shortcut)",
    SectionParent = miscStage2Section,
    Callback = function()
        local obbyStages = workspace:FindFirstChild("ObbyStages")
        if obbyStages then
            local part2 = obbyStages:FindFirstChild("2")
            if part2 and part2:IsA("Part") then
                local touchInterest = part2:FindFirstChild("TouchInterest")
                if touchInterest then
                    firetouchinterest(game.Players.LocalPlayer.Character.HumanoidRootPart, part2, 0)
                    firetouchinterest(game.Players.LocalPlayer.Character.HumanoidRootPart, part2, 1)
                end
                game.Players.LocalPlayer.Character.HumanoidRootPart.CFrame = part2.CFrame
            end
        end
    end
})

local stage3Tab = window:CreateTab("Stage 3")

local landminesSection = stage3Tab:CreateSection("Landmines")

local invincibilityButton = stage3Tab:CreateButton({
    Name = "Invincibility To Landmines (You Can Do The Obby Without Dying)",
    SectionParent = landminesSection,
    Callback = function()
        local stageParts = workspace:FindFirstChild("StageParts")
        if stageParts then
            local folder3 = stageParts:FindFirstChild("3")
            if folder3 then
                local landminesFolder = folder3:FindFirstChild("Landmines")
                if landminesFolder then
                    for _, landmine in ipairs(landminesFolder:GetChildren()) do
                        if landmine.Name == "Landmine" and landmine:IsA("Part") then
                            local touchInterest = landmine:FindFirstChild("TouchInterest")
                            if touchInterest then
                                touchInterest:Destroy()
                            end
                        end
                    end
                end
            end
        end
    end
})

local miscStage3Section = stage3Tab:CreateSection("Misc")

local winObbyButtonStage3 = stage3Tab:CreateButton({
    Name = "Win Obby (If You Are Too Lazy)",
    SectionParent = miscStage3Section,
    Callback = function()
        local obbyStages = workspace:FindFirstChild("ObbyStages")
        if obbyStages then
            local part3 = obbyStages:FindFirstChild("3")
            if part3 and part3:IsA("Part") then
                local touchInterest = part3:FindFirstChild("TouchInterest")
                if touchInterest then
                    firetouchinterest(game.Players.LocalPlayer.Character.HumanoidRootPart, part3, 0)
                    firetouchinterest(game.Players.LocalPlayer.Character.HumanoidRootPart, part3, 1)
                end
                game.Players.LocalPlayer.Character.HumanoidRootPart.CFrame = part3.CFrame
            end
        end
    end
})

local stage4Tab = window:CreateTab("Stage 4")

local fruitObbySection = stage4Tab:CreateSection("Fruit Obby")

local winObbyButton = stage4Tab:CreateButton({
    Name = "Win Obby (Idk How To Remove The Jumpscares)",
    SectionParent = fruitObbySection,
    Callback = function()
        local obbyStages = workspace:FindFirstChild("ObbyStages")
        if obbyStages then
            local part5 = obbyStages:FindFirstChild("5")
            if part5 and part5:IsA("Part") then
                local touchInterest = part5:FindFirstChild("TouchInterest")
                if touchInterest then
                    firetouchinterest(game.Players.LocalPlayer.Character.HumanoidRootPart, part5, 0)
                    firetouchinterest(game.Players.LocalPlayer.Character.HumanoidRootPart, part5, 1)
                end
                game.Players.LocalPlayer.Character.HumanoidRootPart.CFrame = part5.CFrame
            end
        end
    end
})

local stage5Tab = window:CreateTab("Stage 5")

local obbySection = stage5Tab:CreateSection("Obby")

local invincibilityButton = stage5Tab:CreateButton({
    Name = "Invincibility To Killbricks",
    SectionParent = obbySection,
    Callback = function()
        local stageParts = workspace:FindFirstChild("StageParts")
        if stageParts then
            local folder5 = stageParts:FindFirstChild("5")
            if folder5 then
                local killBricksFolder = folder5:FindFirstChild("KillBricks")
                if killBricksFolder then
                    for _, part in ipairs(killBricksFolder:GetChildren()) do
                        if part.Name == "Kill brick" and part:IsA("Part") then
                            local touchInterest = part:FindFirstChild("TouchInterest")
                            if touchInterest then
                                touchInterest:Destroy()
                            end
                        end
                    end
                end
            end
        end
    end
})

local gamepassShopSection = stage5Tab:CreateSection("Gamepass Shop")

local autoRemoveShopToggle = stage5Tab:CreateToggle({
    Name = "Auto Remove Gamepass Shop UI (Annoying UI That Comes On Your Screen)",
    SectionParent = gamepassShopSection,
    CurrentValue = false,
    Flag = "AutoRemoveShop",
    Callback = function(Value)
        local toggleActive = Value
        if toggleActive then
            spawn(function()
                while toggleActive do
                    wait()
                    local players = game:GetService("Players")
                    local localPlayer = players.LocalPlayer
                    if localPlayer then
                        local playerGui = localPlayer:FindFirstChild("PlayerGui")
                        if playerGui then
                            local shopGui = playerGui:FindFirstChild("Shop")
                            if shopGui then
                                shopGui:Destroy()
                            end
                        end
                    end
                end
            end)
        end
    end
})

local miscSection = stage5Tab:CreateSection("Misc")

local winObbyButtonStage5 = stage5Tab:CreateButton({
    Name = "Win Obby (For Lazy Players)",
    SectionParent = miscSection,
    Callback = function()
        local obbyStages = workspace:FindFirstChild("ObbyStages")
        if obbyStages then
            local part5 = obbyStages:FindFirstChild("5")
            if part5 and part5:IsA("Part") then
                local touchInterest = part5:FindFirstChild("TouchInterest")
                if touchInterest then
                    firetouchinterest(game.Players.LocalPlayer.Character.HumanoidRootPart, part5, 0)
                    firetouchinterest(game.Players.LocalPlayer.Character.HumanoidRootPart, part5, 1)
                end
                game.Players.LocalPlayer.Character.HumanoidRootPart.CFrame = part5.CFrame
            end
        end
    end
})
